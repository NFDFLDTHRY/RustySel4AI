# Evidence and claim register — C01/P01

Status vocabulary: `[OBS]` inspected, `[RUN]` executed, `[NEW]` proposed, `[GAP]` missing, `[ERR]` contradiction or failure, `[UNK]` unknown. **NOT RUN** identifies a planned check with no execution result.

Every claim below is bounded to the named source and environment. `[NEW]` and `[GAP]` entries are deliberately not promoted to `[RUN]`.

| ID | Status | Claim / result | Source and raw observation | Limit / STALE_IF |
|---|---|---|---|---|
| O-001 | [OBS] | The owner-named GitHub repository has one observed branch, `main`, at HEAD `f16e023beb0edf6ff4a023e51d1c2e274c3ccb7a` and tree `b2bc0b658356c10caef2069904f715ad896b76df`. | Connected GitHub repository, branches, commit, and recursive tree reads on 2026-09-25 UTC. Tree lists only `README.md` and `LICENSE`. | Remote Git state only; working trees invisible. STALE_IF `main` moves or new files appear. |
| O-002 | [OBS] | The available execution container reports `x86_64`, Linux `6.12.107+deb13-amd64`, Git `2.47.3`, Python `3.13.5`. | `uname`, `git --version`, `python3 --version` at 2026-09-25T06:54:11Z. | This is the task container, not the owner's laptop. STALE_IF environment changes. |
| E-001 | [ERR] | `rustc --version` and `cargo --version` were not usable in this container: rustup attempted to remove `/home/cd/.cargo/bin/rustup-init` and received `Read-only file system`. | Raw stderr: `error: could not remove 'setup' file: '/home/cd/.cargo/bin/rustup-init': Read-only file system (os error 30)`. | No Rust build attempted. A writable isolated toolchain location or another environment is needed before Rust execution claims. STALE_IF toolchain installation changes. |
| E-002 | [ERR] | The first C01 bootstrap check denied the candidate: the local baseline README copy had a trailing newline absent from the pinned 13-byte Git blob, and the evidence vocabulary omitted `[UNK]`. Both were repaired before repeating the check. | External `C01-P01-gate-initial-deny.json` records `baseline Git blob mismatch: README.md`, `baseline Git tree mismatch`, and `evidence status definition missing: [UNK]`. | Retained negative result; not an OS failure. STALE_IF the pinned baseline or gate policy changes. |
| R-001 | [RUN] | The repaired, documentation-only C01 candidate passed the external bootstrap gate and a separate witness. Nine negative cases were denied: outside candidate, outside-path link, symlink, changed LICENSE, missing law, stale base, implementation file, binary document, and tampered receipt. | External receipts `C01-P01-gate.json`, `C01-P01-witness.json`, and `C01-P01-negative-tests.json` are retained with the task's publication record. The gate manifest records each file's SHA-256 and Git blob SHA-1 against O-001. | Bounded to this ten-file documentary candidate and pinned base. It does not prove a future factory, executable isolation, or kernel correctness. STALE_IF any staged byte, gate/witness source, policy, or base changes. |
| I-001–I-005 | [NEW] | Connected design loop, Lace, shared views, factory authority, and native target/provenance are current intended constraints. | [LAW.md](LAW.md), [SYSTEM.md](SYSTEM.md), and exact IDs in [CONTRACTS.md](CONTRACTS.md). | Design only. STALE_IF owner changes intent or experiments contradict a relationship. |
| T-001 | NOT RUN | Physical x86-64 laptop inventory and UEFI boot handoff. | Next experiment in [HANDOFF.md](HANDOFF.md). | No laptop access or boot result in this cycle. |
| T-002 | NOT RUN | Rust `no_std` compilation, compiler output/link audit, or kernel execution. | No source, target, accepted toolchain, or boot artifact yet. | Do not infer from container architecture or tool presence. |

## Gate and publication receipt

The gate and witness were run on the complete candidate after the initial denial was repaired. Their receipts live outside the candidate tree so it cannot edit its judge or rewrite an old result. The publication record for this task carries their hashes and the resulting commit/tree. Re-read the published Git objects and compare their blob/path inventory with the gate manifest before treating C01/P01 as integrated. A later change requires a newly pinned base and fresh receipt.
